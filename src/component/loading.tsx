"use client";

import React from "react";
import { FadeLoader } from "react-spinners";

export default function Loading() {
  return <FadeLoader height={20} width={5} margin={5} color="#041CDE" />;
}
